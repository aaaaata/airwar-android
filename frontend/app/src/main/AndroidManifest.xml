<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AircraftWar">

        <activity android:name=".activity.GameActivity" />
        <activity android:name=".activity.RankActivity" />
        <activity android:name=".activity.MenuRankingActivity" />
        <activity android:name=".activity.GameResultRankActivity" />
        <activity android:name=".activity.PkGameActivity" />
        <activity android:name=".activity.OnlineRoomActivity" />
        <activity android:name=".activity.PkWaitingResultActivity" />
        <activity android:name=".activity.PkResultActivity" />
        <activity android:name=".activity.ModeSelectActivity" />
        <activity android:name=".activity.DifficultySelectActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SkinShopActivity" />

        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>